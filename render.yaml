services:
  - type: web
    name: your-app-name
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn models:app --bind 0.0.0.0:$PORT
    pythonVersion: 3.11.9
