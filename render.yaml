services:
  - type: web
    name: your-app-name
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # or flask run, depending on your setup
    pythonVersion: 3.11.9
